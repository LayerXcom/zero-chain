use std::path::PathBuf;
use crate::term::Term;
use super::error::Result;
// use super::config::Config;

/// Create a new wallet
pub fn new(
    term: &mut Term,
    root_dir: PathBuf,
) -> Result<()> {
    unimplemented!();
}