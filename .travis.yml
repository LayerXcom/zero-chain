sudo: true
language: rust

rust:
  - 1.32.0

cache: cargo

script:
  - sudo apt-get -y update
  - sudo apt-get install -y cmake pkg-config libssl-dev
  - cargo test --release -p bellman-verifier -p zero-chain-crypto -p jubjub -p zero-chain-pairing -p zero-chain-primitives -p zero-chain-proofs -p zero-chain-demo
